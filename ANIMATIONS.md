# GSAP Анимации в Champion School

## Установлено
- `gsap` - основная библиотека анимаций
- `ScrollTrigger` - плагин для анимаций при скролле

## Реализованные анимации

### 1. **Header.tsx**
- ✨ Плавное появление сверху при загрузке страницы
- 🎯 Эффект: `y: -100 → 0`, opacity, 1 секунда
- ⚡ Timing: delay 0.2s

### 2. **HeroSection.tsx** (Главный экран)
- **Заголовок**: Появление снизу с fade-in
- **Подзаголовок**: Появление с задержкой 0.2s
- **Карточки фактов**: Stagger-анимация с увеличением (scale 0.8 → 1), back.out easing
- **Кнопки**: Последовательное появление снизу с задержкой 0.8s

### 3. **PainSection.tsx** (Секция болей)
- **ScrollTrigger**: Анимации запускаются при скролле до 80% высоты экрана
- **Заголовок**: Появление сверху
- **Карточки**: Последовательное появление слева (`x: -30`), stagger 0.1s
- **Заключение**: Увеличение со scale-эффектом

### 4. **SolutionSection.tsx** (Решения)
- **ScrollTrigger активен**
- **Заголовок и подзаголовок**: Fade-in сверху
- **Карточки решений**: 3D-эффект с rotateX, появление снизу, stagger 0.2s
- 🎨 Easing: `back.out(1.4)` для пружинистого эффекта

### 5. **ProgramSection.tsx** (Программа)
- **ScrollTrigger активен**
- **10 модулей**: Последовательное появление слева, stagger 0.08s
- **CTA кнопка**: Увеличение с scale-эффектом `back.out(1.3)`

### 6. **TeamSection.tsx** (Команда лекторов)
- **ScrollTrigger активен**
- **4 карточки**: Появление с увеличением (scale 0.8 → 1)
- **Stagger**: 0.15s между карточками
- 🎯 Easing: `back.out(1.5)`

### 7. **ReviewsSection.tsx** (Отзывы)
- **ScrollTrigger активен**
- **3 отзыва**: 3D-эффект с rotateY: 10 → 0
- **Последовательность**: stagger 0.2s

### 8. **CTASection.tsx** (Финальный призыв к действию)
- **Заголовок**: Масштабирование с пружинистым эффектом `back.out(1.7)`
- **Подзаголовок**: Fade-in с задержкой 0.2s
- **Кнопки**: Увеличение + fade-in, stagger 0.15s, delay 0.4s

### 9. **WhatsAppButton.tsx** (Плавающая кнопка)
- ✨ **Начальная анимация**: Вращение -180° → 0° с увеличением, delay 1s
- 💚 **Пульсация**: Бесконечная анимация scale 1 ↔ 1.1 (yoyo)
- 🎯 **Hover эффект**: Увеличение до scale 1.2 с back.out easing

## Общие паттерны

### Easing функции:
- `power3.out` - плавное замедление (заголовки, текст)
- `back.out(1.7)` - пружинистый эффект (кнопки, карточки)
- `power2.out` - универсальное замедление

### ScrollTrigger настройки:
- `start: 'top 80%'` - анимация начинается когда элемент на 80% высоты экрана
- `start: 'top 75%'` - для карточек (раньше срабатывает)

### Stagger эффекты:
- **0.1s** - быстрая последовательность (боли, модули)
- **0.15s** - средняя (команда, кнопки)
- **0.2s** - медленная (решения, отзывы)

## Производительность

✅ Все анимации используют `gsap.context()` для автоматической очистки
✅ `return () => ctx.revert()` предотвращает утечки памяти
✅ ScrollTrigger оптимизирован для мобильных устройств
✅ Анимации используют GPU-ускорение (transform, opacity)

## Следующие шаги (опционально)

1. Добавить параллакс-эффекты для фона
2. Hover-анимации для карточек программы и команды
3. Анимация счетчиков в StatsSection
4. Магнитный эффект для кнопок (magnetic effect)
5. Cursor trail эффект для desktop

## Команды для разработки

```bash
# Запуск dev сервера
npm run dev

# Проверка типов
npm run build
```
